This project has included a Hydra spec package. This package does not contain executable code.
You must also include the Hydra.Generator package and configure it (via the hydra.xml file)
to generate code from the DLL.
