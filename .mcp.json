{
  "_comment": "Single source of truth for MCP server configuration. Read by both Copilot CLI (workspace .mcp.json) and VS Code 1.102+ via chat.mcp.discovery.enabled. Do not also add .vscode/mcp.json — it causes drift.",
  "mcpServers": {
    "azure-sdk-mcp": {
      "type": "stdio",
      "command": "pwsh",
      "args": ["./eng/common/mcp/azure-sdk-mcp.ps1", "-Run"]
    },
    "generator-agent": {
      "type": "stdio",
      "command": "dotnet",
      "args": [
        "run",
        "--project",
        "sdk/tools/Azure.GeneratorAgent/src/Azure.GeneratorAgent.csproj",
        "--framework",
        "net10.0",
        "--",
        "--mcp-server"
      ]
    }
  }
}
