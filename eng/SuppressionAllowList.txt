# Suppression Allow List
# Format: RuleId:PackagePattern (one entry per line)
# Lines starting with # are comments. Blank lines are ignored.
# Patterns support trailing * wildcard (e.g., Azure.ResourceManager.* matches Azure.ResourceManager.Storage)
#
# A rule only becomes "controlled" when it has at least one entry here.
# Rules not listed → suppression is freely allowed (status quo).
# Rules with entries → only listed packages may suppress them.
#
# The justification and audit trail for each entry lives in the PR that adds it.
