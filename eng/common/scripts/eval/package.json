{
  "name": "@azure-tools/eval-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Pipeline glue scripts for the Vally eval CI (matrix sharding, shard runner, JUnit summary). Synced via eng/common; not published.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
