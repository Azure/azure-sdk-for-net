parameters:
  ServiceDirectory: ''
  AOTTestInputs: []

steps:
- ${{ each aotTestInput in parameters.AOTTestInputs }}:
  - task: Powershell@2
    displayName: Check for AOT compatibility regressions in ${{ aotTestInput.ArtifactName }}
    inputs:
      targetType: filepath
      filePath: eng/common/aotcompatibility/Check-AOT-Compatibility.ps1
      arguments: >-
        -ServiceDirectory ${{ parameters.ServiceDirectory }}
        -PackageName ${{ aotTestInput.ArtifactName }}
        -ExpectedWarningsFilePath ${{ aotTestInput.ExpectedAOTWarningsFilepath }}
      workingDirectory: eng/common/aotcompatibility