steps:
  - task: Npm@1
    inputs:
      command: custom
      customCommand: install --prefix $(Agent.TempDirectory)/azurite azurite
    displayName: Install Azurite
    env:
      AzureWebJobsStorageAzuriteLocation: $(Agent.TempDirectory)