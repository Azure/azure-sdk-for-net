# Azure.Security.ConfidentialLedger approved analyzer suppressions.
# See eng/analyzerallowlist/README.md for the file format and review policy.

# AZC0005: The ConfidentialLedgerCertificateClient custom code suppresses the
# generated constructors and provides its own that don't take credentials
# (the identity service is public). The generated protected parameterless
# constructor is suppressed as part of the custom constructor set.
nowarn:AZC0005

# AZC0012: Generated model type names (Bundle, Collection, Constitution,
# Metadata, Mode, Role) match the Confidential Ledger service API terminology.
# Renaming them would break alignment with the REST API and confuse users
# who reference the service documentation. These names are auto-generated
# from the TypeSpec specification and cannot be changed without diverging
# from the API contract.
nowarn:AZC0012

# AZC0030: Generated model 'Collection' ends with 'Collection' and
# 'UserDefinedFunctionExecutionResponse' ends with 'Response'. Both names
# are derived directly from the TypeSpec/REST API schema and renaming them
# would create a mismatch with the service contract.
nowarn:AZC0030
