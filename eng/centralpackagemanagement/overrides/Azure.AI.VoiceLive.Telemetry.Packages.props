<Project>
  <!-- Azure.Identity is not resolved from the central CPM for this project;
       declared here via the per-project override mechanism. -->
  <ItemGroup>
    <PackageVersion Include="Azure.Identity" Version="1.20.0" />
  </ItemGroup>
</Project>
