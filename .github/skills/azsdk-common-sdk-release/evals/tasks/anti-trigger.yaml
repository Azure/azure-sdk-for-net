id: anti-trigger
name: anti-trigger
description: Should not trigger on unrelated prompts
inputs:
  prompt: "How do I write unit tests for my Azure SDK client?"
expected:
  skill_activated: false
graders:
  - type: text
    name: no-release-advice
    config:
      not_contains:
        - "release process"
        - "publish package"
