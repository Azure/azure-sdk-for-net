// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

#nullable enable
// <auto-generated>
// This file was generated by scripts/generate-validators.py from the OpenAPI spec.
// Do not edit manually. Re-run scripts/Generate-Contracts.ps1 to regenerate.
// </auto-generated>

using System.Text.Json;
using Azure.AI.AgentServer.Responses;

namespace Azure.AI.AgentServer.Responses.Validators;

/// <summary>
/// Validates JSON payloads against the API schema.
/// Extend via partial class in Custom/Validators/ to add custom rules.
/// </summary>
internal static partial class ContainerAutoParamValidator
{
    /// <summary>
    /// Validates a parsed JSON element.
    /// </summary>
    public static ValidationResult Validate(JsonElement element)
    {
        var errors = new List<ValidationError>();

        if (element.ValueKind != JsonValueKind.Object)
        {
            errors.Add(new ValidationError("$", $"Expected object, got {element.ValueKind}"));
            return ValidationResult.Failure(errors);
        }

        // Optional: file_ids
        if (element.TryGetProperty("file_ids", out var fileIdsProp))
        {
            if (fileIdsProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.file_ids", $"Expected array, got {fileIdsProp.ValueKind}"));
        }

        // Optional: memory_limit
        if (element.TryGetProperty("memory_limit", out var memoryLimitProp) && memoryLimitProp.ValueKind != JsonValueKind.Null)
        {
            if (memoryLimitProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.memory_limit", $"Expected string, got {memoryLimitProp.ValueKind}"));
            else if (memoryLimitProp.GetString() is not ("1g" or "4g" or "16g" or "64g"))
                errors.Add(new ValidationError("$.memory_limit", $"Value '{memoryLimitProp.GetString()}' is not valid. Allowed: 1g, 4g, 16g, 64g"));
        }

        // Optional: network_policy
        if (element.TryGetProperty("network_policy", out var networkPolicyProp))
        {
            var networkPolicyResult = ContainerNetworkPolicyParamValidator.Validate(networkPolicyProp);
            if (!networkPolicyResult.IsValid)
            {
                foreach (var e in networkPolicyResult.Errors)
                    errors.Add(new ValidationError("$.network_policy" + e.Path.Substring(1), e.Message));
            }
        }

        // Optional: skills
        if (element.TryGetProperty("skills", out var skillsProp))
        {
            if (skillsProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.skills", $"Expected array, got {skillsProp.ValueKind}"));
            else
            {
                var skillsIdx = 0;
                foreach (var item in skillsProp.EnumerateArray())
                {
                    var itemResult = ContainerSkillValidator.Validate(item);
                    if (!itemResult.IsValid)
                    {
                        foreach (var e in itemResult.Errors)
                            errors.Add(new ValidationError($"$.skills[{skillsIdx}]" + e.Path.Substring(1), e.Message));
                    }
                    skillsIdx++;
                }
            }
        }

        // Required: type
        if (!element.TryGetProperty("type", out var typeValProp))
            errors.Add(new ValidationError("$.type", "Required property 'type' is missing"));
        else
        {
            if (typeValProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.type", $"Expected string, got {typeValProp.ValueKind}"));
            else if (typeValProp.GetString() is not ("container_auto"))
                errors.Add(new ValidationError("$.type", $"Value '{typeValProp.GetString()}' is not valid. Allowed: container_auto"));
        }

        ValidateCustom(element, errors);

        return errors.Count == 0 ? ValidationResult.Success : ValidationResult.Failure(errors);
    }

    /// <summary>
    /// Extension point for custom validation rules.
    /// </summary>
    static partial void ValidateCustom(JsonElement element, List<ValidationError> errors);
}
