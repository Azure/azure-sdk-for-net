// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

#nullable enable
// <auto-generated>
// This file was generated by scripts/generate-validators.py from the OpenAPI spec.
// Do not edit manually. Re-run scripts/Generate-Contracts.ps1 to regenerate.
// </auto-generated>

using System.Text.Json;
using Azure.AI.AgentServer.Responses;

namespace Azure.AI.AgentServer.Responses.Validators;

/// <summary>
/// Validates JSON payloads against the API schema.
/// Extend via partial class in Custom/Validators/ to add custom rules.
/// </summary>
internal static partial class ItemReasoningItemValidator
{
    /// <summary>
    /// Validates a parsed JSON element.
    /// </summary>
    public static ValidationResult Validate(JsonElement element)
    {
        var errors = new List<ValidationError>();

        if (element.ValueKind != JsonValueKind.Object)
        {
            errors.Add(new ValidationError("$", $"Expected object, got {element.ValueKind}"));
            return ValidationResult.Failure(errors);
        }

        // Optional: content
        if (element.TryGetProperty("content", out var contentProp))
        {
            if (contentProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.content", $"Expected array, got {contentProp.ValueKind}"));
            else
            {
                var contentIdx = 0;
                foreach (var item in contentProp.EnumerateArray())
                {
                    var itemResult = ReasoningTextContentValidator.Validate(item);
                    if (!itemResult.IsValid)
                    {
                        foreach (var e in itemResult.Errors)
                            errors.Add(new ValidationError($"$.content[{contentIdx}]" + e.Path.Substring(1), e.Message));
                    }
                    contentIdx++;
                }
            }
        }

        // Optional: encrypted_content
        if (element.TryGetProperty("encrypted_content", out var encryptedContentProp) && encryptedContentProp.ValueKind != JsonValueKind.Null)
        {
            if (encryptedContentProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.encrypted_content", $"Expected string, got {encryptedContentProp.ValueKind}"));
        }

        // Optional: id
        if (element.TryGetProperty("id", out var idProp) && idProp.ValueKind != JsonValueKind.Null)
        {
            if (idProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.id", $"Expected string, got {idProp.ValueKind}"));
        }

        // Optional: status
        if (element.TryGetProperty("status", out var statusProp))
        {
            if (statusProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.status", $"Expected string, got {statusProp.ValueKind}"));
            else if (statusProp.GetString() is not ("in_progress" or "completed" or "incomplete"))
                errors.Add(new ValidationError("$.status", $"Value '{statusProp.GetString()}' is not valid. Allowed: in_progress, completed, incomplete"));
        }

        // Required: summary
        if (!element.TryGetProperty("summary", out var summaryProp))
            errors.Add(new ValidationError("$.summary", "Required property 'summary' is missing"));
        else
        {
            if (summaryProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.summary", $"Expected array, got {summaryProp.ValueKind}"));
            else
            {
                var summaryIdx = 0;
                foreach (var item in summaryProp.EnumerateArray())
                {
                    var itemResult = SummaryTextContentValidator.Validate(item);
                    if (!itemResult.IsValid)
                    {
                        foreach (var e in itemResult.Errors)
                            errors.Add(new ValidationError($"$.summary[{summaryIdx}]" + e.Path.Substring(1), e.Message));
                    }
                    summaryIdx++;
                }
            }
        }

        // Required: type
        if (!element.TryGetProperty("type", out var typeValProp))
            errors.Add(new ValidationError("$.type", "Required property 'type' is missing"));
        else
        {
            if (typeValProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.type", $"Expected string, got {typeValProp.ValueKind}"));
            else if (typeValProp.GetString() is not ("reasoning"))
                errors.Add(new ValidationError("$.type", $"Value '{typeValProp.GetString()}' is not valid. Allowed: reasoning"));
        }

        ValidateCustom(element, errors);

        return errors.Count == 0 ? ValidationResult.Success : ValidationResult.Failure(errors);
    }

    /// <summary>
    /// Extension point for custom validation rules.
    /// </summary>
    static partial void ValidateCustom(JsonElement element, List<ValidationError> errors);
}
