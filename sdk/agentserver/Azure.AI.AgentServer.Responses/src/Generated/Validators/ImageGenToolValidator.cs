// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

#nullable enable
// <auto-generated>
// This file was generated by scripts/generate-validators.py from the OpenAPI spec.
// Do not edit manually. Re-run scripts/Generate-Contracts.ps1 to regenerate.
// </auto-generated>

using System.Text.Json;
using Azure.AI.AgentServer.Responses;

namespace Azure.AI.AgentServer.Responses.Validators;

/// <summary>
/// Validates JSON payloads against the API schema.
/// Extend via partial class in Custom/Validators/ to add custom rules.
/// </summary>
internal static partial class ImageGenToolValidator
{
    /// <summary>
    /// Validates a parsed JSON element.
    /// </summary>
    public static ValidationResult Validate(JsonElement element)
    {
        var errors = new List<ValidationError>();

        if (element.ValueKind != JsonValueKind.Object)
        {
            errors.Add(new ValidationError("$", $"Expected object, got {element.ValueKind}"));
            return ValidationResult.Failure(errors);
        }

        // Optional: action
        if (element.TryGetProperty("action", out var actionProp))
        {
            if (actionProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.action", $"Expected string, got {actionProp.ValueKind}"));
            else if (actionProp.GetString() is not ("generate" or "edit" or "auto"))
                errors.Add(new ValidationError("$.action", $"Value '{actionProp.GetString()}' is not valid. Allowed: generate, edit, auto"));
        }

        // Optional: background
        if (element.TryGetProperty("background", out var backgroundProp))
        {
            if (backgroundProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.background", $"Expected string, got {backgroundProp.ValueKind}"));
            else if (backgroundProp.GetString() is not ("transparent" or "opaque" or "auto"))
                errors.Add(new ValidationError("$.background", $"Value '{backgroundProp.GetString()}' is not valid. Allowed: transparent, opaque, auto"));
        }

        // Optional: description
        if (element.TryGetProperty("description", out var descriptionProp))
        {
            if (descriptionProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.description", $"Expected string, got {descriptionProp.ValueKind}"));
        }

        // Optional: input_fidelity
        if (element.TryGetProperty("input_fidelity", out var inputFidelityProp) && inputFidelityProp.ValueKind != JsonValueKind.Null)
        {
            if (inputFidelityProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.input_fidelity", $"Expected string, got {inputFidelityProp.ValueKind}"));
            else if (inputFidelityProp.GetString() is not ("high" or "low"))
                errors.Add(new ValidationError("$.input_fidelity", $"Value '{inputFidelityProp.GetString()}' is not valid. Allowed: high, low"));
        }

        // Optional: input_image_mask
        if (element.TryGetProperty("input_image_mask", out var inputImageMaskProp))
        {
            var inputImageMaskResult = ImageGenToolInputImageMaskValidator.Validate(inputImageMaskProp);
            if (!inputImageMaskResult.IsValid)
            {
                foreach (var e in inputImageMaskResult.Errors)
                    errors.Add(new ValidationError("$.input_image_mask" + e.Path.Substring(1), e.Message));
            }
        }

        // Optional: model
        if (element.TryGetProperty("model", out var modelProp))
        {
            if (modelProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.model", $"Expected string, got {modelProp.ValueKind}"));
        }

        // Optional: moderation
        if (element.TryGetProperty("moderation", out var moderationProp))
        {
            if (moderationProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.moderation", $"Expected string, got {moderationProp.ValueKind}"));
            else if (moderationProp.GetString() is not ("auto" or "low"))
                errors.Add(new ValidationError("$.moderation", $"Value '{moderationProp.GetString()}' is not valid. Allowed: auto, low"));
        }

        // Optional: name
        if (element.TryGetProperty("name", out var nameProp))
        {
            if (nameProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.name", $"Expected string, got {nameProp.ValueKind}"));
        }

        // Optional: output_compression
        if (element.TryGetProperty("output_compression", out var outputCompressionProp))
        {
            if (outputCompressionProp.ValueKind != JsonValueKind.Number)
                errors.Add(new ValidationError("$.output_compression", $"Expected integer, got {outputCompressionProp.ValueKind}"));
            else
            {
                var val = outputCompressionProp.GetDouble();
                if (val < 0 || val > 100)
                    errors.Add(new ValidationError("$.output_compression", $"Value {val} is outside allowed range [0, 100]"));
            }
        }

        // Optional: output_format
        if (element.TryGetProperty("output_format", out var outputFormatProp))
        {
            if (outputFormatProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.output_format", $"Expected string, got {outputFormatProp.ValueKind}"));
            else if (outputFormatProp.GetString() is not ("png" or "webp" or "jpeg"))
                errors.Add(new ValidationError("$.output_format", $"Value '{outputFormatProp.GetString()}' is not valid. Allowed: png, webp, jpeg"));
        }

        // Optional: partial_images
        if (element.TryGetProperty("partial_images", out var partialImagesProp))
        {
            if (partialImagesProp.ValueKind != JsonValueKind.Number)
                errors.Add(new ValidationError("$.partial_images", $"Expected integer, got {partialImagesProp.ValueKind}"));
            else
            {
                var val = partialImagesProp.GetDouble();
                if (val < 0 || val > 3)
                    errors.Add(new ValidationError("$.partial_images", $"Value {val} is outside allowed range [0, 3]"));
            }
        }

        // Optional: quality
        if (element.TryGetProperty("quality", out var qualityProp))
        {
            if (qualityProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.quality", $"Expected string, got {qualityProp.ValueKind}"));
            else if (qualityProp.GetString() is not ("low" or "medium" or "high" or "auto"))
                errors.Add(new ValidationError("$.quality", $"Value '{qualityProp.GetString()}' is not valid. Allowed: low, medium, high, auto"));
        }

        // Optional: size
        if (element.TryGetProperty("size", out var sizeProp))
        {
            if (sizeProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.size", $"Expected string, got {sizeProp.ValueKind}"));
            else if (sizeProp.GetString() is not ("1024x1024" or "1024x1536" or "1536x1024" or "auto"))
                errors.Add(new ValidationError("$.size", $"Value '{sizeProp.GetString()}' is not valid. Allowed: 1024x1024, 1024x1536, 1536x1024, auto"));
        }

        // Required: type
        if (!element.TryGetProperty("type", out var typeValProp))
            errors.Add(new ValidationError("$.type", "Required property 'type' is missing"));
        else
        {
            if (typeValProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.type", $"Expected string, got {typeValProp.ValueKind}"));
            else if (typeValProp.GetString() is not ("image_generation"))
                errors.Add(new ValidationError("$.type", $"Value '{typeValProp.GetString()}' is not valid. Allowed: image_generation"));
        }

        ValidateCustom(element, errors);

        return errors.Count == 0 ? ValidationResult.Success : ValidationResult.Failure(errors);
    }

    /// <summary>
    /// Extension point for custom validation rules.
    /// </summary>
    static partial void ValidateCustom(JsonElement element, List<ValidationError> errors);
}
