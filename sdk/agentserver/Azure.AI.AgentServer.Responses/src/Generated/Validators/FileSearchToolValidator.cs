// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

#nullable enable
// <auto-generated>
// This file was generated by scripts/generate-validators.py from the OpenAPI spec.
// Do not edit manually. Re-run scripts/Generate-Contracts.ps1 to regenerate.
// </auto-generated>

using System.Text.Json;
using Azure.AI.AgentServer.Responses;

namespace Azure.AI.AgentServer.Responses.Validators;

/// <summary>
/// Validates JSON payloads against the API schema.
/// Extend via partial class in Custom/Validators/ to add custom rules.
/// </summary>
internal static partial class FileSearchToolValidator
{
    /// <summary>
    /// Validates a parsed JSON element.
    /// </summary>
    public static ValidationResult Validate(JsonElement element)
    {
        var errors = new List<ValidationError>();

        if (element.ValueKind != JsonValueKind.Object)
        {
            errors.Add(new ValidationError("$", $"Expected object, got {element.ValueKind}"));
            return ValidationResult.Failure(errors);
        }

        // Optional: description
        if (element.TryGetProperty("description", out var descriptionProp))
        {
            if (descriptionProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.description", $"Expected string, got {descriptionProp.ValueKind}"));
        }

        // Optional: filters
        if (element.TryGetProperty("filters", out var filtersProp) && filtersProp.ValueKind != JsonValueKind.Null)
        {
            var filtersResult = FiltersValidator.Validate(filtersProp);
            if (!filtersResult.IsValid)
            {
                foreach (var e in filtersResult.Errors)
                    errors.Add(new ValidationError("$.filters" + e.Path.Substring(1), e.Message));
            }
        }

        // Optional: max_num_results
        if (element.TryGetProperty("max_num_results", out var maxNumResultsProp))
        {
            if (maxNumResultsProp.ValueKind != JsonValueKind.Number)
                errors.Add(new ValidationError("$.max_num_results", $"Expected integer, got {maxNumResultsProp.ValueKind}"));
        }

        // Optional: name
        if (element.TryGetProperty("name", out var nameProp))
        {
            if (nameProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.name", $"Expected string, got {nameProp.ValueKind}"));
        }

        // Optional: ranking_options
        if (element.TryGetProperty("ranking_options", out var rankingOptionsProp))
        {
            var rankingOptionsResult = RankingOptionsValidator.Validate(rankingOptionsProp);
            if (!rankingOptionsResult.IsValid)
            {
                foreach (var e in rankingOptionsResult.Errors)
                    errors.Add(new ValidationError("$.ranking_options" + e.Path.Substring(1), e.Message));
            }
        }

        // Required: type
        if (!element.TryGetProperty("type", out var typeValProp))
            errors.Add(new ValidationError("$.type", "Required property 'type' is missing"));
        else
        {
            if (typeValProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.type", $"Expected string, got {typeValProp.ValueKind}"));
            else if (typeValProp.GetString() is not ("file_search"))
                errors.Add(new ValidationError("$.type", $"Value '{typeValProp.GetString()}' is not valid. Allowed: file_search"));
        }

        // Required: vector_store_ids
        if (!element.TryGetProperty("vector_store_ids", out var vectorStoreIdsProp))
            errors.Add(new ValidationError("$.vector_store_ids", "Required property 'vector_store_ids' is missing"));
        else
        {
            if (vectorStoreIdsProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.vector_store_ids", $"Expected array, got {vectorStoreIdsProp.ValueKind}"));
        }

        ValidateCustom(element, errors);

        return errors.Count == 0 ? ValidationResult.Success : ValidationResult.Failure(errors);
    }

    /// <summary>
    /// Extension point for custom validation rules.
    /// </summary>
    static partial void ValidateCustom(JsonElement element, List<ValidationError> errors);
}
