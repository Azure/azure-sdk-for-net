// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

#nullable enable
// <auto-generated>
// This file was generated by scripts/generate-validators.py from the OpenAPI spec.
// Do not edit manually. Re-run scripts/Generate-Contracts.ps1 to regenerate.
// </auto-generated>

using System.Text.Json;
using Azure.AI.AgentServer.Responses;

namespace Azure.AI.AgentServer.Responses.Validators;

/// <summary>
/// Validates JSON payloads against the API schema.
/// Extend via partial class in Custom/Validators/ to add custom rules.
/// </summary>
internal static partial class ItemFileSearchToolCallValidator
{
    /// <summary>
    /// Validates a parsed JSON element.
    /// </summary>
    public static ValidationResult Validate(JsonElement element)
    {
        var errors = new List<ValidationError>();

        if (element.ValueKind != JsonValueKind.Object)
        {
            errors.Add(new ValidationError("$", $"Expected object, got {element.ValueKind}"));
            return ValidationResult.Failure(errors);
        }

        // Required: id
        if (!element.TryGetProperty("id", out var idProp))
            errors.Add(new ValidationError("$.id", "Required property 'id' is missing"));
        else
        {
            if (idProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.id", $"Expected string, got {idProp.ValueKind}"));
        }

        // Required: queries
        if (!element.TryGetProperty("queries", out var queriesProp))
            errors.Add(new ValidationError("$.queries", "Required property 'queries' is missing"));
        else
        {
            if (queriesProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.queries", $"Expected array, got {queriesProp.ValueKind}"));
        }

        // Optional: results
        if (element.TryGetProperty("results", out var resultsProp) && resultsProp.ValueKind != JsonValueKind.Null)
        {
            if (resultsProp.ValueKind != JsonValueKind.Array)
                errors.Add(new ValidationError("$.results", $"Expected array, got {resultsProp.ValueKind}"));
            else
            {
                var resultsIdx = 0;
                foreach (var item in resultsProp.EnumerateArray())
                {
                    var itemResult = FileSearchToolCallResultsValidator.Validate(item);
                    if (!itemResult.IsValid)
                    {
                        foreach (var e in itemResult.Errors)
                            errors.Add(new ValidationError($"$.results[{resultsIdx}]" + e.Path.Substring(1), e.Message));
                    }
                    resultsIdx++;
                }
            }
        }

        // Required: status
        if (!element.TryGetProperty("status", out var statusProp))
            errors.Add(new ValidationError("$.status", "Required property 'status' is missing"));
        else
        {
            if (statusProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.status", $"Expected string, got {statusProp.ValueKind}"));
            else if (statusProp.GetString() is not ("in_progress" or "searching" or "completed" or "incomplete" or "failed"))
                errors.Add(new ValidationError("$.status", $"Value '{statusProp.GetString()}' is not valid. Allowed: in_progress, searching, completed, incomplete, failed"));
        }

        // Required: type
        if (!element.TryGetProperty("type", out var typeValProp))
            errors.Add(new ValidationError("$.type", "Required property 'type' is missing"));
        else
        {
            if (typeValProp.ValueKind != JsonValueKind.String)
                errors.Add(new ValidationError("$.type", $"Expected string, got {typeValProp.ValueKind}"));
            else if (typeValProp.GetString() is not ("file_search_call"))
                errors.Add(new ValidationError("$.type", $"Value '{typeValProp.GetString()}' is not valid. Allowed: file_search_call"));
        }

        ValidateCustom(element, errors);

        return errors.Count == 0 ? ValidationResult.Success : ValidationResult.Failure(errors);
    }

    /// <summary>
    /// Extension point for custom validation rules.
    /// </summary>
    static partial void ValidateCustom(JsonElement element, List<ValidationError> errors);
}
