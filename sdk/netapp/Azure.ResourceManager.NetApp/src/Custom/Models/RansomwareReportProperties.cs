// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
namespace Azure.ResourceManager.NetApp.Models
{
    /// <summary> Ransomware report properties. </summary>
    public partial class RansomwareReportProperties
    {
        // The GENERATED RansomwareReportProperties has only an internal ctor (every
        // property is read-only / output-only). The public parameterless ctor below is
        // added so user code (and test harnesses) can construct an empty instance for
        // mocking/round-tripping scenarios that rely on JsonModel deserialization.
        // Without it, callers would have no way to create the type at all.
        /// <summary> Initializes a new instance of <see cref="RansomwareReportProperties"/> for deserialization. </summary>
        public RansomwareReportProperties()
        {
        }
    }
}
