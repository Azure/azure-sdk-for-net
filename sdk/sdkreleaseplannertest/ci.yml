# NOTE: Please refer to https://aka.ms/azsdk/engsys/ci-yaml before editing this file.

trigger:
  branches:
    include:
    - main
    - hotfix/*
    - release/*
  paths:
    include:
    - sdk/sdkreleaseplannertest/

pr:
  branches:
    include:
    - main
    - feature/*
    - hotfix/*
    - release/*
  paths:
    include:
    - sdk/sdkreleaseplannertest/

extends:
  template: /eng/pipelines/templates/stages/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: sdkreleaseplannertest
    ArtifactName: packages
    Artifacts:
    - name: Azure.Security.SDKReleasePlannerTest
      safeName: AzureSecuritySDKReleasePlannerTest
