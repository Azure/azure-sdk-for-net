trigger: none

extends:
  template: ../../eng/pipelines/templates/stages/archetype-sdk-tests.yml
  parameters:
    EnvVars:
      CONVERSATIONS_KEY: $(conversations-key)
      CONVERSATIONS_PROJECT: cognitiveLanguageSdkConversation
      CONVERSATIONS_URI: $(conversations-uri)
      QUESTIONANSWERING_KEY: $(questionanswering-key)
      QUESTIONANSWERING_PROJECT: cognitiveLanguageSdkQnA
      QUESTIONANSWERING_ENDPOINT: $(questionanswering-uri)
    ServiceDirectory: cognitivelanguage
