trigger: none

extends:
  template: ../../eng/pipelines/templates/jobs/archetype-sdk-tests-multiple.yml
  parameters:
    jobs:
      # Azure Cloud
      - ServiceDirectory: tables
        ScenarioName: AzureCloud
        SubscriptionConfiguration: $(sub-config-azure-cloud-test-resources)
        EnvVars:
          AZURE_AUTHORITY_HOST: https://login.microsoftonline.com
          TABLES_STORAGE_ENDPOINT_SUFFIX: table.core.windows.net
          TABLES_COSMOS_ENDPOINT_SUFFIX: table.cosmos.azure.com

      # AzureUSGovernment
      - ServiceDirectory: tables
        ScenarioName: AzureUSGovernment
        SubscriptionConfiguration: $(sub-config-gov-test-resources)
        EnvVars:
          AZURE_AUTHORITY_HOST: https://login.microsoftonline.us
          TABLES_STORAGE_ENDPOINT_SUFFIX: table.core.usgovcloudapi.net
          TABLES_COSMOS_ENDPOINT_SUFFIX: table.cosmos.azure.us

      # AzureChinaCloud
      - ServiceDirectory: tables
        ScenarioName: AzureChinaCloud
        SubscriptionConfiguration: $(sub-config-cn-test-resources)
        EnvVars:
          AZURE_AUTHORITY_HOST: https://login.chinacloudapi.cn
          TABLES_STORAGE_ENDPOINT_SUFFIX: table.core.chinacloudapi.cn
          TABLES_COSMOS_ENDPOINT_SUFFIX: table.cosmos.azure.cn
