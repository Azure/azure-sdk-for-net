<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyTitle>Microsoft Cognitive Services Form Recognizer SDK</AssemblyTitle>
    <Description>This client library provides access to the Microsoft Cognitive Services Form Recognizer APIs.</Description>
    <VersionPrefix>0.9.0</VersionPrefix>
    <PackageTags>FormRecognizer;Form Recognizer;</PackageTags>
    <PackageReleaseNotes>
<![CDATA[
This is a public preview release of the Cognitive Services Form Recognizer SDK.
This release also contains Cognitive Services Form Recognizer Receipt SDK.

  Form Recognizer API Changes:
    TrainRequest::TrainRequest() constructor: Added new optional parameter(s):
        TrainSourceFilter: To set filter to further search the source path for content.

]]>
	</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Properties\AssemblyInfo.cs" />
  </ItemGroup>

  <ItemGroup>
    <Folder Include="Properties\" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFrameworks>$(RequiredTargetFrameworks)</TargetFrameworks>
  </PropertyGroup>
  
  <PropertyGroup>
    <AzureApiTag />
    <PackageTags>$(PackageTags);$(CommonTags);$(AzureApiTag);</PackageTags>
  </PropertyGroup>
</Project>