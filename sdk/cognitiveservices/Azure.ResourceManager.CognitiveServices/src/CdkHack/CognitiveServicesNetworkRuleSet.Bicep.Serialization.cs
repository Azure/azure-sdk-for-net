// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.

// <auto-generated/>

#nullable disable

using System;
using System.Text;
using System.Text.Json;
using Azure.Core;
using Azure.Core.Serialization;

namespace Azure.ResourceManager.CognitiveServices.Models
{
    public partial class CognitiveServicesNetworkRuleSet : IModelJsonSerializable<CognitiveServicesNetworkRuleSet>
    {
        void IModelJsonSerializable<CognitiveServicesNetworkRuleSet>.Serialize(Utf8JsonWriter writer, ModelSerializerOptions options) => ((IUtf8JsonSerializable)this).Write(writer);

        CognitiveServicesNetworkRuleSet IModelJsonSerializable<CognitiveServicesNetworkRuleSet>.Deserialize(ref Utf8JsonReader reader, ModelSerializerOptions options)
        {
            using var doc = JsonDocument.ParseValue(ref reader);
            return DeserializeCognitiveServicesNetworkRuleSet(doc.RootElement);
        }

        BinaryData IModelSerializable<CognitiveServicesNetworkRuleSet>.Serialize(ModelSerializerOptions options) => (options.Format.ToString()) switch
        {
            "J" or "W" => ModelSerializer.SerializeCore(this, options),
            "bicep" => SerializeBicep(options),
            _ => throw new FormatException($"Unsupported format {options.Format}")
        };

        CognitiveServicesNetworkRuleSet IModelSerializable<CognitiveServicesNetworkRuleSet>.Deserialize(BinaryData data, ModelSerializerOptions options)
        {
            using var document = JsonDocument.Parse(data);
            return DeserializeCognitiveServicesNetworkRuleSet(document.RootElement);
        }

        private BinaryData SerializeBicep(ModelSerializerOptions options)
        {
            StringBuilder sb = new StringBuilder();
            if (Optional.IsDefined(DefaultAction))
            {
                sb.AppendLine($"  defaultAction: '{DefaultAction.Value}'");
            }
            if (Optional.IsCollectionDefined(IPRules))
            {
                sb.AppendLine($"  ipRules: [");
                foreach (var item in IPRules)
                {
                    sb.AppendLine($"    {{");
                    sb.AppendChildObject(item, options, true);
                    sb.AppendLine($"    }}");
                }
                sb.AppendLine($"  ]");
            }
            if (Optional.IsCollectionDefined(VirtualNetworkRules))
            {
                sb.AppendLine($"  virtualNetworkRules: [");
                foreach (var item in VirtualNetworkRules)
                {
                    sb.AppendLine($"    {{");
                    sb.AppendChildObject(item, options, true);
                    sb.AppendLine($"    }}");
                }
                sb.AppendLine($"  ]");
            }
            return BinaryData.FromString(sb.ToString());
        }
    }
}
