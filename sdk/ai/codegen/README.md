# The plugin to set experimental attributes

This plugin visits the classes and properties, detects `@extension` decorator, containing `x-ms-foundry-meta` key and marks classes and properties as experimrntal.
