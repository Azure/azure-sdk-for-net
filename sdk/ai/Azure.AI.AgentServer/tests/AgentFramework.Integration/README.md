# Agent Framework Adapter Integration Test
This is a integration test project for the Agent Framework adapters.

## Prerequisites
- Following `environment_variables.json.template` to create a `environment_variable.json` file containing the necessary environment variables for the adapters being tested.
- Run tests and view the results from Test Explorer in Visual Studio or using the .NET CLI.
