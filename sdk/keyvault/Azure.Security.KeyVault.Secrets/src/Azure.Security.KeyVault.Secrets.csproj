<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>This is the Microsoft Azure Key Vault Secrets client library</Description>
    <AssemblyTitle>Microsoft Azure.Security.KeyVault.Secrets client library</AssemblyTitle>
    <Version>4.12.0-beta.1</Version>
    <!--The ApiCompatVersion is managed automatically and should not generally be modified manually.-->
    <ApiCompatVersion>4.11.0</ApiCompatVersion>
    <PackageTags>Microsoft Azure Key Vault Secrets;$(PackageCommonTags)</PackageTags>

    <TargetFrameworks>$(RequiredTargetFrameworks)</TargetFrameworks>
    <NoWarn>$(NoWarn);3021</NoWarn>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <IncludeOperationsSharedSource>true</IncludeOperationsSharedSource>
  </PropertyGroup>

  <Import Project="..\..\Azure.Security.KeyVault.Shared\src\Azure.Security.KeyVault.Shared.projitems" Label="Shared" />

  <ItemGroup>
    <PackageReference Include="Azure.Core" />
  </ItemGroup>

  <!--
    Auto-generated TypeSpec output (see ..\tsp-location.yaml) lives under
    .\Generated. The Internalize-Generated.ps1 step hides those types from the
    public API surface — the customer-facing client is the hand-written
    SecretClient. This mirrors Java's `implementation.models` subpackage and
    Python's `_generated` namespace conventions.

    Two hooks:
      1. Pre-Compile on every local build (with Inputs/Outputs so incremental
         builds don't re-run the script when nothing changed).
      2. AfterTargets="GenerateCode" so when CI's "Verify Generated Code"
         step (or a developer's `dotnet build /t:GenerateCode`) regenerates
         the public types, they are immediately re-internalized — keeping
         the on-disk Generated/ tree in sync with what's committed.
  -->
  <Target Name="InternalizeGeneratedCode"
          BeforeTargets="CoreCompile"
          Inputs="$(MSBuildThisFileDirectory)Generated\**\*.cs;$(MSBuildThisFileDirectory)..\eng\Internalize-Generated.ps1"
          Outputs="$(IntermediateOutputPath)InternalizeGeneratedCode.stamp">
    <Exec Command="pwsh -NoProfile -ExecutionPolicy Bypass -File &quot;$(MSBuildThisFileDirectory)..\eng\Internalize-Generated.ps1&quot;"
          ConsoleToMSBuild="true"
          IgnoreExitCode="false" />
    <Touch Files="$(IntermediateOutputPath)InternalizeGeneratedCode.stamp" AlwaysCreate="true" />
  </Target>

  <Target Name="InternalizeGeneratedCodeAfterGenerate"
          AfterTargets="GenerateCode">
    <Exec Command="pwsh -NoProfile -ExecutionPolicy Bypass -File &quot;$(MSBuildThisFileDirectory)..\eng\Internalize-Generated.ps1&quot;"
          ConsoleToMSBuild="true"
          IgnoreExitCode="false" />
  </Target>

  <ItemGroup>
    <Compile Include="$(AzureCoreSharedSources)AzureResourceProviderNamespaceAttribute.cs" LinkBase="Shared\Core" />
    <Compile Include="$(AzureCoreSharedSources)ArrayBufferWriter.cs" LinkBase="Shared\Core" />
    <Compile Include="$(AzureCoreSharedSources)CallerShouldAuditAttribute.cs" LinkBase="Shared\Core" />
    <Compile Include="$(AzureCoreSharedSources)ExperimentalAttribute.cs" LinkBase="Shared\Core" />
    <Compile Include="$(AzureCoreSharedSources)Argument.cs" LinkBase="Shared\Core" />
  </ItemGroup>

</Project>
