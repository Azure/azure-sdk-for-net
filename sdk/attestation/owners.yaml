# Generated by codeowners-migration convert.
# Paths are relative to this directory and may not escape it.

version: 1

paths:
  - path: Azure.Security.Attestation/
    owners: [Azure/azure-sdk-write-attestation, gkostal]
    pr-labels: [Attestation]
