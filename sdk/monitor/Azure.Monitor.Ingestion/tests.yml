trigger: none

stages:
  - template: /eng/pipelines/templates/stages/archetype-sdk-tests.yml
    parameters:
      ServiceDirectory: monitor
      Artifacts:
        - name: azure-monitor-ingestion
          groupId: com.azure
          safeName: azure-monitor-ingestion
