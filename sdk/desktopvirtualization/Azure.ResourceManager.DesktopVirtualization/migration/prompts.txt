Use migration/handle_renamemapping.md to handle rename mapping.
Use migration/handle_rp_prefix.md to handle prepend-rp-prefix.
Use migration/handle_format_by_name_rules.md to handle format-by-name-rules.




Run `dotnet build` and review all reported breaking changes.

Your task is to analyze each breaking change and produce a remediation plan.

For every breaking change:
1. Identify the root cause.
2. Propose a fix using one of the following approaches:
   - SDK-side fix: modify custom code in the SDK.
   - TSP-side fix: modify `client.tsp`.

Guidelines:
- Prefer TSP-side fixes whenever possible.
- Do NOT make any actual code changes.
- Only produce a plan describing how each issue should be fixed.
- Be specific about what needs to be changed (files, decorators, types, or code areas).

Output:
- Write the full plan to a file named `fixing_plan.txt`.
- Organize the file as:
  - One section per breaking change
  - Include:
    - Error message
    - Root cause analysis
    - Preferred fix (TSP-side or SDK-side)
    - Detailed steps to implement the fix



The current SDK is generated from the spec repo at
/home/codespace/code/spec/specification/desktopvirtualization/resource-manager/Microsoft.DesktopVirtualization/DesktopVirtualization
using the script ./migration/regen-local-no-tsp-client.sh.

There is an issue where the Disconnect method is not generated into UserSessionResource.cs, which causes an API-level breaking change.

To reproduce and investigate the issue:

I added the following TypeSpec file into the test TypeSpec project:
/home/codespace/code/sdk/eng/packages/http-client-csharp-mgmt/generator/TestProjects/Local/Mgmt-TypeSpec/UserSession.tsp

I then ran the generator script:
/home/codespace/code/sdk/eng/packages/http-client-csharp-mgmt/eng/scripts/generate.sh

In the generated test project, the Disconnect method is correctly generated into UserSessionResource.cs.

This behavior is unexpected because both the service SDK and the local test project are generated using the same local emitter and local generator, yet the method placement differs.

Your task is to perform a careful comparison and root-cause analysis to explain why the Disconnect method is generated into the correct resource class (UserSessionResource.cs) in the local test project but not in the service SDK.

Focus on:

Differences in TypeSpec inputs (especially resource definitions, operation binding, decorators, and namespaces)

Differences in generator configuration or pipeline inputs

Any transformation or normalization steps that may differ between the two runs

Resource mapping or operation grouping logic that could affect method placement

Provide a concrete explanation of the root cause rather than listing superficial differences.