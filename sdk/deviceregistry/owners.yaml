# Generated by codeowners-migration convert.
# Paths are relative to this directory and may not escape it.

version: 1

paths:
  - path: Azure.ResourceManager.DeviceRegistry/
    owners: [atastrophic, rohankhandelwal, siongithub, mryanlo]
    pr-labels: [Device Registry]
    section: Management Libraries
