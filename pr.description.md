# PR scenario results

| Scenario | Expected result | Build link | Notes |
| --- | --- | --- | --- |
| S1 | `Azure.Core` is discovered in `PackageInfo` and skipped for CODEOWNERS because package info marks it with `ArtifactDetails.skipCodeownersVerification` | https://dev.azure.com/azure-sdk/29ec6040-b234-4e31-b139-33dc4287b756/_build/results?buildId=6253825 | `Verify Codeowners` succeeded in logs `837` and `1308`; log `837` contains `Skipping package: Azure.Core sdk/core/Azure.Core because package info marks it to skip CODEOWNERS verification.` |
| S2 | `Azure.Template` is discovered as a non-ignored existing package, reaches CODEOWNERS validation, and fails because no CODEOWNERS entry matches the package directory | https://dev.azure.com/azure-sdk/29ec6040-b234-4e31-b139-33dc4287b756/_build/results?buildId=6254393 | `Verify Codeowners` failed in log `617` with `PR context: not skipping CODEOWNERS for 'sdk/template/Azure.Template' because the target branch already contains files under that directory.` followed by `check-package failed: No CODEOWNERS entry matches path 'sdk/template/Azure.Template'.` |
| S3 | `Azure.Template` is included indirectly from `sdk/template/ci.yml`, but CODEOWNERS is skipped because the PR does not directly change files under `sdk/template/Azure.Template/` | https://dev.azure.com/azure-sdk/29ec6040-b234-4e31-b139-33dc4287b756/_build/results?buildId=6254522 | `Verify Codeowners` succeeded in log `542` with `Validating codeowners for package: Azure.Template sdk/template/Azure.Template` followed by `PR context: skipping CODEOWNERS for 'sdk/template/Azure.Template' because the PR does not directly change files under that package directory.` |
| S4 | `Azure.Template2` is included in `PackageInfo` as a brand-new package directory, `Verify Readmes` passes after copying the Azure.Template README content, and CODEOWNERS is skipped because the directory does not yet exist on the target branch | https://dev.azure.com/azure-sdk/29ec6040-b234-4e31-b139-33dc4287b756/_build/results?buildId=6255230 | `Save package properties filtered for PR` succeeded in logs `66` and `74` with `Including 'Azure.Template2' because of changed file '/mnt/vss/_work/1/s/sdk/template/Azure.Template2/src'.` and emitted `Azure.Template2.json`; `Verify Readmes` succeeded in log `363`; `Verify Codeowners` succeeded in log `693` with `PR context: skipping CODEOWNERS for 'sdk/template/Azure.Template2' because the PR only introduces files in a brand-new directory.` |
